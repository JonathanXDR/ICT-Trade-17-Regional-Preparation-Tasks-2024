# yaml-language-server: $schema=https://schema.skills17.ch/task-config/v3/config.schema.json
id: css-selectors
metadata:
  name: CSS Selectors
  testsLink: http://localhost/work/htmlcss/tests.html
serve:
  enabled: true
localHistory: true
source:
- src/task.css
groups:
- match: CSS Selectors.+
  displayName: CSS Selectors
  tests:
    - match: Task 1
      points: 0.5
    - match: Task 2
      points: 0.5
    - match: Task 5
      points: 0.5
